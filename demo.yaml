name: jaato Interactive Client Demo
timeout: 120

steps:
  - type: "What Python files are in the current directory?"
    permission: "y"

  - type: "Show me the git status"
    permission: "a"

  - type: "quit"
    delay: 0.08
